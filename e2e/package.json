{
  "name": "baia-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end Playwright tests for the BAIA pipeline",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install:browsers": "playwright install chromium --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "typescript": "~5.4.0"
  }
}
